"use client";

import { Car } from "@/types/car";

interface Props {
  listings: Car[];
}

export default function HeroSectionClient({ listings }: Props) {
  return null; // Placeholder for client-side logic
}
